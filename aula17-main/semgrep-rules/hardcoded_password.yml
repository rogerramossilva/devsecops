rules:
  - id: hardcoded-password-assignment
    pattern: $VAR = "$VAL"
    message: "Possível senha hardcoded detectada: $VAR"
    languages: [python]
    severity: ERROR
    metadata:
      category: security
    condition: $VAR ~= "(?i)(password|passwd|pwd|secret|token)"

